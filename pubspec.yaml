name: nice_overlay
description: A package for controlling overlay components such as in-app notification, toast, and snack bar.
version: 1.0.0
homepage: https://github.com/terry1213/nice_overlay
issue_tracker: https://github.com/terry1213/nice_overlay/issues

environment:
  sdk: '>=3.0.6 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
